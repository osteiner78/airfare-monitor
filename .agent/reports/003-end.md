# 003 — End Report

## What was built

Ten UX corrections across backend and frontend:

| # | Fix | What changed |
|---|-----|-------------|
| 1 | Uppercase form POST | `.upper().strip()` on `form["origin"]` and `form["destination"]` in `pages.py` add_tracker route |
| 2 | Pause updates detail header | Created `detail_page.html` partial (header + content in `#detail-content` wrapper). `toggle-detail` route returns it. `tracker.html` includes it for initial load. |
| 3 | Chart date format | `searched_at` values now use `"T"` separator (ISO 8601) for reliable Chart.js time scale parsing |
| 4 | Wider detail container | Added `.detail-container { max-width: 960px }` to CSS. Applied to `tracker.html`. |
| 5 | Airline name column | `_map_result` now uses `r.primary_airline_name` as the `airline` field. Results table shows airline name and flight number in separate columns. |
| 6 | Arrival date column | Results table now has Dep Date, Dep Time, Arr Date, Arr Time columns. Renders `arrival_time_date` and `arrival_time_time` from context. |
| 7 | Search Now at top | Moved Search Now button from `detail_content.html` results section to `detail_page.html` header controls alongside Pause/Delete. |
| 8 | Card layout restructure | Route+price in header row. Date+badge in body row. Delta+last-checked+Pause/Delete in footer row. |
| 9 | Booking URLs | `_map_result` now constructs `https://www.google.com/travel/flights?q=...` search URL from origin/dest/date. Passes origin/dest/date through to `_map_result`. |
| 10 | Timezone offset preservation | Time display now includes UTC offset (e.g., `09:45 +02:00`). Extracted from ISO 8601 timestamps with `+/-HH:MM` offsets. |

## Verification output

Regression: **87 passed, 1 deselected** (slow/live).

```
tests/ -k "not slow"
======================= 87 passed, 1 deselected in 1.50s =======================
```

Smoke test:

```
=== Fix 1: Uppercase form POST ===
Uppercased GVA: True
Uppercased BCN: True
=== Fix 4: Wider detail container ===
Has detail-container: True
=== Fix 7: Search Now in header ===
Search Now in detail-controls: True
=== Fix 8: Card layout has price in header ===
Best price in card-header: True
=== Detail page still works ===
status=200
=== Toggle-detail still works ===
status=200
=== Search-now still works ===
status=200
```

## Commit

`f9ccb24` — 9 files changed, 139 insertions, 58 deletions.

## Deviations from plan

None. All 10 fixes exactly as specified.

## Test gaps

None. Plan 003 has no new tests; non-regression verified via existing suite.

## Follow-ups

- The booking URL is a Google Flights search URL, not a true booking deep-link. `booking_token` from fli could enable direct booking links if a URL format is discovered.
- The scheduler job exception during smoke test was a test artifact (DB deleted while server running) — not a production issue.

## Confidence

**certain** — all 87 tests pass, smoke tests confirm key fixes.
